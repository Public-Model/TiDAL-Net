# Contributing

1. Create a feature branch.
2. Add or update tests for every behavioral change.
3. Run `pytest -q` and `ruff check .`.
4. Do not commit raw private telemetry, credentials, or unlicensed third-party data.
5. Mark approximations and review defaults explicitly; never present them as paper-exact settings.
