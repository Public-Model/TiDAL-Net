base: configs/base.yaml
datasets:
  - configs/datasets/msl.yaml
  - configs/datasets/smap.yaml
  - configs/datasets/esa_adb.yaml
corruption: configs/corruption/reality_informed.yaml
ratios: [0.03, 0.05, 0.08, 0.10]
seeds:
  start: 2026
  count: 30
reuse_identical_masks_across_methods: true
models:
  - tidalnet
  - persistence
  - linear_interpolation
  - gru_reconstructor
# Third-party baselines are registered in configs/baselines/registry.yaml and must be
# installed from their licensed upstream implementations before paper-exact runs.
