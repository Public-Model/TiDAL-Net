# Third-party baselines

Do not copy upstream baseline code here without checking its license. For the final release, provide one subdirectory or environment file per baseline, pin the upstream URL and commit hash, and implement only the task adapter required by `docs/BASELINE_ADAPTATION.md`.
